# nftban-managed: yes
# nftban-templates-version: 1
# Focus on brute attempts via xmlrpc; refine to your log format.

[Definition]
failregex = ^<HOST> - .* "POST /xmlrpc\.php.*" (200|401|403) .*
ignoreregex =
