# SPDX-License-Identifier: MPL-2.0
# Dependabot configuration for automated dependency updates
# https://docs.github.com/en/code-security/dependabot/dependabot-version-updates

version: 2

# Enable automatic security updates (creates PRs for CVEs)
# This is in addition to version updates below

updates:
  # Go modules - SECURITY CRITICAL
  - package-ecosystem: "gomod"
    directory: "/"
    schedule:
      interval: "daily"  # Daily for faster CVE response
    open-pull-requests-limit: 10
    commit-message:
      prefix: "deps(go)"
    labels:
      - "dependencies"
      - "go"
      - "security"
    # Group minor/patch updates to reduce PR noise
    groups:
      go-minor-patch:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"
    # Prioritize security updates
    reviewers:
      - "itcmsgr"
    assignees:
      - "itcmsgr"

  # GitHub Actions - SUPPLY CHAIN SECURITY
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      # Weekly, NOT daily. Action bumps are overwhelmingly minor/patch digest churn,
      # and every open Dependabot PR re-runs the full check graph. gomod deliberately
      # stays DAILY above: CVE response time in shipped Go dependencies is a different
      # question from CI churn, and is not traded away here.
      interval: "weekly"
      day: "monday"
    # Bounded queue. Was 10; four action PRs sat open simultaneously, each
    # re-running the whole graph.
    open-pull-requests-limit: 3
    # Do not auto-rebase open PRs: every rebase re-triggers the full graph.
    rebase-strategy: "disabled"
    commit-message:
      prefix: "deps(actions)"
    labels:
      - "dependencies"
      - "github-actions"
      - "security"
    groups:
      actions-minor-patch:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"

  # Docker base images
  - package-ecosystem: "docker"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 3
    commit-message:
      prefix: "deps(docker)"
    labels:
      - "dependencies"
      - "docker"

  # TODO v2.x: Uncomment when GUI adds package.json
  # - package-ecosystem: "npm"
  #   directory: "/"
  #   schedule:
  #     interval: "weekly"
  #     day: "monday"
  #   open-pull-requests-limit: 3
  #   commit-message:
  #     prefix: "deps(npm)"
  #   labels:
  #     - "dependencies"
  #     - "npm"
