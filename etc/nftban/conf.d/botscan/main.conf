# =============================================================================
# NFTBan Bot Scanner - MAIN Configuration
# =============================================================================
# SPDX-License-Identifier: MPL-2.0
# meta:name="botscan_main_config"
# meta:type="config"
# meta:owner="Antonios Voulvoulis <contact@nftban.com>"
# meta:created_date="2026-01-11"
# meta:description="Main configuration for bot scanner module"
# meta:input="None"
# meta:output="Bot scanner module settings"
# meta:depends=""
# meta:inventory.files=""
# meta:inventory.binaries=""
# meta:inventory.env_vars=""
# meta:inventory.config_files=""
# meta:inventory.systemd_units=""
# meta:inventory.network=""
# meta:inventory.privileges="none"
# =============================================================================

# =============================================================================
# MODULE CONTROL
# =============================================================================

# Enable/disable bot scanner module
BOTSCAN_ENABLED="true"

# Action mode: alert | ban | both
#   alert = DETECT-ONLY (logs matches, does NOT ban)
#   ban   = enforce (write bans to blacklist_manual_*)
#   both  = enforce (identical to 'ban' in the current engine)
BOTSCAN_ACTION_MODE="both"

# =============================================================================
# LOG SOURCES
# =============================================================================

# Auto-detect log files
BOTSCAN_LOG_AUTO="true"

# Apache access logs
BOTSCAN_LOG_APACHE="/var/log/apache2/access.log"
BOTSCAN_LOG_APACHE_ALT="/var/log/httpd/access_log"

# Nginx access logs
BOTSCAN_LOG_NGINX="/var/log/nginx/access.log"

# Combined log format expected
# LogFormat "%h %l %u %t \"%r\" %>s %b \"%{Referer}i\" \"%{User-Agent}i\""

# =============================================================================
# DETECTION SETTINGS
# =============================================================================

# Default thresholds (can be overridden per-pattern)
BOTSCAN_DEFAULT_THRESHOLD="5"
BOTSCAN_DEFAULT_WINDOW="60"
BOTSCAN_DEFAULT_BAN_SHORT="1800"
BOTSCAN_DEFAULT_BAN_LONG="7200"

# 404 tracking - ban IPs with excessive 404s
BOTSCAN_404_TRACKING="true"
BOTSCAN_404_THRESHOLD="50"
BOTSCAN_404_WINDOW="300"
BOTSCAN_404_BAN="3600"

# Progressive banning
BOTSCAN_PROGRESSIVE_ENABLED="true"
BOTSCAN_PROGRESSIVE_MULTIPLIER="2"
BOTSCAN_PROGRESSIVE_MAX="86400"

# =============================================================================
# PATTERN FILES
# =============================================================================

# Pattern directory
BOTSCAN_PATTERNS_DIR="${NFTBAN_CONFIG_DIR:-/etc/nftban}/patterns.d/botscan"

# Pattern files
# NON-FUNCTIONAL / RESERVED (v1.219.0): the loader globs ALL patterns.d/botscan/*.patterns;
# this key is NOT read and does not select files. Left for documentation only.
BOTSCAN_PATTERN_FILES="webshell.patterns,exploit.patterns,scanner.patterns,custom.patterns"

# --- Scan safety / budget knobs (read by the scanner; document them here) ---
# BOTSCAN_SCAN_BUDGET_SECS — per-cycle soft deadline in seconds (0 = time-unbounded; the
#   processor timer path defaults to 180). Lower it on busy hosts to bound cycle time.
# BOTSCAN_SCAN_MAX_BYTES_PER_FILE / BOTSCAN_404_TAIL_TOTAL_BYTES — per-file / 404-tail read caps
#   that bound memory (the v1.209.3 spool-OOM class). These gate the exact blow-ups BotScan guards.
#BOTSCAN_SCAN_BUDGET_SECS="180"

# =============================================================================
# WHITELIST
# =============================================================================

# Whitelist known good bots (comma-separated, case-insensitive)
BOTSCAN_WHITELIST_BOTS="googlebot,bingbot,yandexbot,duckduckbot,slurp,facebot"

# Whitelist paths (regex, never trigger on these)
BOTSCAN_WHITELIST_PATHS="/robots\.txt|/favicon\.ico|/sitemap\.xml|/ads\.txt"

# Use global NFTBan whitelist
BOTSCAN_USE_GLOBAL_WHITELIST="true"

# =============================================================================
# STATE & LOGGING
# =============================================================================

# State file for tracking
BOTSCAN_STATE_FILE="${NFTBAN_DATA_DIR:-/var/lib/nftban}/botscan-state.db"

# Log file
BOTSCAN_LOG_FILE="${NFTBAN_LOG_DIR:-/var/log/nftban}/botscan.log"

# Debug mode
BOTSCAN_DEBUG="false"

# =============================================================================
# INTEGRATION
# =============================================================================

# Send to Prometheus metrics
# NON-FUNCTIONAL / RESERVED (v1.219.0): not read by the scanner. No effect.
BOTSCAN_PROMETHEUS_ENABLED="true"

# GeoIP enrichment
# NON-FUNCTIONAL / RESERVED (v1.219.0): not read by the scanner. No effect.
BOTSCAN_GEOIP_ENABLED="true"

# Alert notifications
# NON-FUNCTIONAL / RESERVED (v1.219.0): not read by the scanner. Detect-only vs ban is
# controlled by BOTSCAN_ACTION_MODE (alert/ban/both), not by this key. No effect.
BOTSCAN_ALERT_ENABLED="true"
