# =============================================================================
# NFTBan v1.0.0 - System Watchdog Configuration
# =============================================================================
# SPDX-License-Identifier: MPL-2.0
# meta:name="watchdog.conf"
# meta:type="config"
# meta:owner="Antonios Voulvoulis <contact@nftban.com>"
# meta:created_date="2025-11-05"
# meta:description="Monitor system resources for troubleshooting and auditing"
# meta:inventory.files=""
# meta:inventory.binaries=""
# meta:inventory.env_vars=""
# meta:inventory.config_files="/etc/nftban/conf.d/watchdog.conf"
# meta:inventory.systemd_units="nftban-watchdog.timer"
# meta:inventory.network=""
# meta:inventory.privileges="none"
#
# This module monitors OS-level metrics NOT covered by other modules:
# - Load Average (from /proc/loadavg)
# - Memory/Swap Usage (from /proc/meminfo)
# - I/O Wait % (from /proc/stat)
# - Disk Usage (df command)
# - File Descriptors (from /proc/sys/fs/file-nr)
# - Top Processes (CPU and memory consumers)
#
# What OTHER modules handle (we don't touch):
# - Connection per IP: nftban ddos (ct count in nftables)
# - Ban rate alerts: nftban stats
# - Service health: nftban health
# =============================================================================

# -----------------------------------------------------------------------------
# GENERAL SETTINGS
# -----------------------------------------------------------------------------
# Enable/disable watchdog (use: nftban watchdog enable/disable)
# Default: true - system resource monitoring is critical for any server
NFTBAN_WATCHDOG_ENABLED="true"

# Check interval in seconds (default: 90s for responsive alerting)
# CSF uses 60s, we use 90s for slightly less overhead
NFTBAN_WATCHDOG_INTERVAL="90"

# Alert throttle - don't repeat same alert within this time (seconds)
# Default: 1 hour (3600s) - same as CSF
NFTBAN_WATCHDOG_ALERT_THROTTLE="3600"

# -----------------------------------------------------------------------------
# LOAD AVERAGE MONITORING
# -----------------------------------------------------------------------------
NFTBAN_WATCHDOG_LOAD_ENABLED="true"

# Which load average to check: 1, 5, or 15 (minute)
# Recommendation: 5 (balances responsiveness with stability)
NFTBAN_WATCHDOG_LOAD_AVG="5"

# Warning threshold (will generate WARNING alert)
NFTBAN_WATCHDOG_LOAD_WARNING="10"

# Critical threshold (will generate CRITICAL alert)
NFTBAN_WATCHDOG_LOAD_CRITICAL="20"

# -----------------------------------------------------------------------------
# MEMORY MONITORING
# -----------------------------------------------------------------------------
NFTBAN_WATCHDOG_MEM_ENABLED="true"

# Memory usage warning threshold (percentage)
NFTBAN_WATCHDOG_MEM_WARNING="80"

# Memory usage critical threshold (percentage)
NFTBAN_WATCHDOG_MEM_CRITICAL="95"

# Swap usage warning threshold (percentage)
# Swap usage often indicates memory pressure
NFTBAN_WATCHDOG_SWAP_WARNING="50"

# -----------------------------------------------------------------------------
# I/O WAIT MONITORING (Disk Bottleneck Detection)
# -----------------------------------------------------------------------------
NFTBAN_WATCHDOG_IOWAIT_ENABLED="true"

# I/O wait warning threshold (percentage)
# High I/O wait indicates disk is bottleneck
NFTBAN_WATCHDOG_IOWAIT_WARNING="20"

# I/O wait critical threshold (percentage)
NFTBAN_WATCHDOG_IOWAIT_CRITICAL="40"

# -----------------------------------------------------------------------------
# DISK USAGE MONITORING
# -----------------------------------------------------------------------------
NFTBAN_WATCHDOG_DISK_ENABLED="true"

# Path to monitor for disk usage (usually log directory)
# Can be comma-separated for multiple paths: "/var/log,/var/lib"
NFTBAN_WATCHDOG_DISK_PATH="/var/log"

# Disk usage warning threshold (percentage)
NFTBAN_WATCHDOG_DISK_WARNING="80"

# Disk usage critical threshold (percentage)
NFTBAN_WATCHDOG_DISK_CRITICAL="95"

# -----------------------------------------------------------------------------
# PROCESS TRACKING (Top Resource Consumers)
# -----------------------------------------------------------------------------
NFTBAN_WATCHDOG_PROC_ENABLED="true"

# Number of top processes to track in reports
NFTBAN_WATCHDOG_PROC_TOP_COUNT="10"

# Alert if single process exceeds this CPU percentage
NFTBAN_WATCHDOG_PROC_CPU_ALERT="80"

# Alert if single process exceeds this memory (in MB)
NFTBAN_WATCHDOG_PROC_MEM_ALERT="2048"

# -----------------------------------------------------------------------------
# ALERTS & LOGGING
# -----------------------------------------------------------------------------
# Watchdog log file
NFTBAN_WATCHDOG_LOG="/var/log/nftban/watchdog.log"

# Directory for saved reports (for auditing)
NFTBAN_WATCHDOG_REPORT_DIR="/var/lib/nftban/reports/watchdog"

# Send email on critical alerts (uses system mail)
NFTBAN_WATCHDOG_ALERT_EMAIL="true"

# Log alerts to syslog (journald)
NFTBAN_WATCHDOG_ALERT_SYSLOG="true"

# -----------------------------------------------------------------------------
# PROMETHEUS METRICS (for History and Graphing)
# -----------------------------------------------------------------------------
# Export metrics in Prometheus format
NFTBAN_WATCHDOG_METRICS_ENABLED="true"

# Prometheus metrics file (picked up by node_exporter textfile collector)
NFTBAN_WATCHDOG_METRICS_FILE="/var/lib/nftban/metrics/watchdog.prom"

# Days to keep reports (for auditing)
NFTBAN_WATCHDOG_REPORT_RETENTION="7"

# -----------------------------------------------------------------------------
# RUNTIME STATS COLLECTION (Daemon Performance Monitoring)
# DEPRECATED v1.54.0: These keys are not read by any code. The Go daemon
# uses internal defaults. Kept for reference; will be removed in v1.56.0.
# -----------------------------------------------------------------------------
# Enable runtime stats collection from nftband daemon
# When disabled: NO behavioral change, NO background work
#NFTBAN_WATCHDOG_STATS_ENABLED="true"

# Stats collection frequency (seconds)
# - Live metrics (RAM, goroutines): every 60s (identifies memory leaks)
# - I/O & Events (bans/min): every 300s (5 min for trends)
#NFTBAN_WATCHDOG_STATS_INTERVAL_LIVE="60"
#NFTBAN_WATCHDOG_STATS_INTERVAL_IO="300"

# Stats file locations (atomic JSON writes: temp + fsync + rename)
#NFTBAN_WATCHDOG_STATS_CURRENT="/var/lib/nftban/stats/current.json"
#NFTBAN_WATCHDOG_STATS_HISTORY_DIR="/var/lib/nftban/stats/history"

# History retention (days) - HARD LIMIT enforced
#NFTBAN_WATCHDOG_STATS_RETENTION_DAYS="30"

# -----------------------------------------------------------------------------
# RUNTIME THRESHOLDS (Alerts without external backend)
# DEPRECATED v1.54.0: See above.
# -----------------------------------------------------------------------------
# Daemon memory threshold (MB) - warn if heap exceeds
#NFTBAN_WATCHDOG_STATS_MEMORY_WARN="200"
#NFTBAN_WATCHDOG_STATS_MEMORY_CRIT="500"

# Goroutine count threshold - indicates leak if exceeded
#NFTBAN_WATCHDOG_STATS_GOROUTINES_WARN="100"
#NFTBAN_WATCHDOG_STATS_GOROUTINES_CRIT="500"

# Stale stats alert (seconds) - if current.json older than this
# CLI reports "STALE" warning when exceeded
#NFTBAN_WATCHDOG_STATS_STALE_THRESHOLD="300"

# -----------------------------------------------------------------------------
# PROFILING (pprof) - For performance troubleshooting
# -----------------------------------------------------------------------------
# Enable pprof profiling (SECURITY: disabled by default)
# When enabled, pprof binds to localhost only (127.0.0.1:6060)
# Requires daemon restart with --profile flag
NFTBAN_WATCHDOG_PROFILE_ENABLED="false"

# Profile storage directory
NFTBAN_WATCHDOG_PROFILE_DIR="/var/lib/nftban/stats/profiles"

# Profile retention (days) - auto-delete old profiles - HARD LIMIT
NFTBAN_WATCHDOG_PROFILE_RETENTION_DAYS="7"

# Max profiles to keep (prevents disk fill) - HARD LIMIT
NFTBAN_WATCHDOG_PROFILE_MAX_COUNT="10"

# Auto-capture on threshold breach (experimental - use with caution)
NFTBAN_WATCHDOG_PROFILE_AUTO_CAPTURE="false"

# -----------------------------------------------------------------------------
# DEDICATED WATCHDOG LOGGING
# -----------------------------------------------------------------------------
# Dedicated watchdog log directory (separate from main watchdog.log)
NFTBAN_WATCHDOG_LOG_DIR="/var/log/nftban/watchdog"
#NFTBAN_WATCHDOG_STATS_LOG="/var/log/nftban/watchdog/stats.log"  # DEPRECATED v1.54.0
NFTBAN_WATCHDOG_ALERTS_LOG="/var/log/nftban/watchdog/alerts.log"
NFTBAN_WATCHDOG_PROFILE_LOG="/var/log/nftban/watchdog/profiles.log"

# -----------------------------------------------------------------------------
# JSONL TREND LOGGING (Fast Append-Only for Trend Analysis)
# -----------------------------------------------------------------------------
# JSONL file for per-cycle trend logging (one JSON object per line)
# Written after EVERY watchdog cycle for granular trend analysis
# Auto-rotates at max lines (keeps most recent data)
NFTBAN_WATCHDOG_TRENDS_JSONL="/var/log/nftban/watchdog/trends.jsonl"

# Maximum lines before rotation (~7 days at 90s intervals = ~6700 entries)
NFTBAN_WATCHDOG_TRENDS_MAX_LINES="10000"

# Lines to keep after rotation (approximately 3.5 days of data)
NFTBAN_WATCHDOG_TRENDS_KEEP_LINES="5000"

# =============================================================================
# DYNAMIC WATCHDOG (Auto-Alignment System)
# =============================================================================
# The dynamic watchdog continuously monitors pressure across four dimensions:
# - CPU: Process CPU%, system load, softnet drops
# - MEM: RSS vs budget, heap vs GOMEMLIMIT, GC fraction
# - IO:  iowait%, disk usage
# - NET: conntrack utilization, softnet drops rate
#
# Operating Modes:
# - NORMAL:   All OK - full functionality
# - DEGRADED: Any WARN - reduced frequency, backpressure enabled
# - SURVIVAL: Any CRITICAL - essential operations only
# =============================================================================

# Enable dynamic watchdog (auto-alignment based on pressure)
NFTBAN_DYNAMIC_WATCHDOG_ENABLED="true"

# Base collection interval (adjusts dynamically based on mode)
NFTBAN_DYNAMIC_WATCHDOG_INTERVAL="5"

# -----------------------------------------------------------------------------
# HYSTERESIS SETTINGS (Prevents Mode Flapping)
# -----------------------------------------------------------------------------
# Hysteresis thresholds (0-100 score)
# Enter WARN at this score
NFTBAN_PRESSURE_WARN_ENTER="60"
# Exit WARN when below this for 30s
NFTBAN_PRESSURE_WARN_EXIT="50"
# Enter CRITICAL at this score
NFTBAN_PRESSURE_CRIT_ENTER="80"
# Exit CRITICAL when below this for 60s
NFTBAN_PRESSURE_CRIT_EXIT="70"

# Duration required to exit levels (seconds)
NFTBAN_PRESSURE_WARN_EXIT_DURATION="30"
NFTBAN_PRESSURE_CRIT_EXIT_DURATION="60"

# -----------------------------------------------------------------------------
# BUDGET/THRESHOLD SETTINGS
# -----------------------------------------------------------------------------
# Memory budget (bytes) - align with GOMEMLIMIT if set
# Default: 1GB (1073741824)
NFTBAN_MEM_BUDGET_BYTES="1073741824"

# RSS critical threshold as percentage of budget
NFTBAN_RSS_CRIT_PERCENT="95"

# Heap critical threshold as percentage of GOMEMLIMIT
NFTBAN_HEAP_CRIT_PERCENT="90"

# CPU critical threshold (process CPU%)
NFTBAN_CPU_CRIT_PERCENT="80"

# Load average critical threshold (normalized by CPU count)
NFTBAN_LOAD_NORMALIZED_CRIT="4.0"

# I/O wait critical threshold (percentage)
NFTBAN_IOWAIT_CRIT_PERCENT="40"

# Disk usage critical threshold (percentage)
NFTBAN_DISK_CRIT_PERCENT="95"

# Conntrack utilization critical threshold (0.0-1.0)
NFTBAN_CONNTRACK_UTIL_CRIT="0.9"

# Softnet drops rate critical threshold (drops/second)
NFTBAN_SOFTNET_DROPS_RATE_CRIT="100"

# Goroutine count critical threshold
NFTBAN_GOROUTINES_CRIT="10000"

# -----------------------------------------------------------------------------
# AUTO-PROFILING SETTINGS
# -----------------------------------------------------------------------------
# Enable automatic profiling on threshold breach
NFTBAN_PROFILE_AUTO_ENABLED="true"

# Cooldowns between profile captures (seconds)
NFTBAN_PROFILE_CPU_COOLDOWN="900"    # 15 minutes
NFTBAN_PROFILE_HEAP_COOLDOWN="1800"  # 30 minutes
NFTBAN_PROFILE_GOROUTINE_COOLDOWN="300"  # 5 minutes

# CPU profile duration (seconds)
NFTBAN_PROFILE_CPU_DURATION="30"

# -----------------------------------------------------------------------------
# MEMORY VALVE (debug.FreeOSMemory)
# -----------------------------------------------------------------------------
# Enable memory valve (calls debug.FreeOSMemory under safe conditions)
NFTBAN_FREE_OS_MEMORY_ENABLED="true"

# Cooldown between FreeOSMemory calls (seconds)
NFTBAN_FREE_OS_MEMORY_COOLDOWN="600"  # 10 minutes

# Only trigger if CPU is below this percentage
NFTBAN_FREE_OS_MEMORY_ONLY_IF_CPU_BELOW="40"

# -----------------------------------------------------------------------------
# DEGRADATION SETTINGS
# -----------------------------------------------------------------------------
# Disable expensive nft ruleset scanning in degraded mode
NFTBAN_DEGRADE_DISABLE_NFT_RULESET_SCAN="true"

# Reduce workers in degraded mode
NFTBAN_DEGRADE_REDUCE_WORKERS_TO="2"

# Reduce workers in survival mode
NFTBAN_SURVIVAL_REDUCE_WORKERS_TO="1"

# Interval multiplier in degraded mode
NFTBAN_DEGRADED_INTERVAL_MULTIPLIER="2.0"

# -----------------------------------------------------------------------------
# FLIGHT RECORDER SETTINGS
# -----------------------------------------------------------------------------
# Enable flight recorder for post-mortem analysis
NFTBAN_RECORDER_ENABLED="true"

# Flight recorder directory
NFTBAN_RECORDER_DIR="/var/lib/nftban/recorder"

# Maximum events in ring buffer
NFTBAN_RECORDER_MAX_EVENTS="1000"

# Snapshot interval (seconds)
NFTBAN_RECORDER_SNAPSHOT_INTERVAL="60"

# Days to keep old recorder files
NFTBAN_RECORDER_RETENTION_DAYS="7"

# =============================================================================
# USAGE
# =============================================================================
# Enable:   nftban watchdog enable
# Disable:  nftban watchdog disable
# Status:   nftban watchdog status
# Check:    nftban watchdog check
# Report:   nftban watchdog report
# History:  nftban watchdog history
# Stats:    nftban watchdog stats [--json]
# Snapshot: nftban watchdog snapshot [--heap|--cpu N]
# Profiles: nftban watchdog profiles
# Pressure: nftban watchdog pressure [--json]
# Events:   nftban watchdog events [--count N]
# =============================================================================
