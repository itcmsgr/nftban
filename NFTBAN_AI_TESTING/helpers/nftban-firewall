#!/usr/bin/env bash
# SPDX-License-Identifier: MPL-2.0
# nftban-firewall - Firewall status collector
# Part of NFTBan v0.30 - Self-healing monitoring system

set -euo pipefail

# Dump nftables ruleset in JSON format
if command -v nft >/dev/null 2>&1; then
    nft -j list ruleset 2>/dev/null || echo '{}'
else
    echo '{}'
fi
